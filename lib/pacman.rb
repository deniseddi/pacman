class Pacman #PLACE
    def initialize(x,y,f)
        @x = x
        @y = y 
        @facing = f
    end

    def # there should be a method where pacman knows where it is at all times and show/output to REPORT
        @x,@y,@facing
    end 
end